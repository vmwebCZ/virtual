<VirtualHost *:80>
	ServerName admin.dev

	ServerAdmin webmaster@localhost
	DocumentRoot /var/www/admin

	#LogLevel info ssl:warn

	ErrorLog ${APACHE_LOG_DIR}/admin.error.log
	CustomLog ${APACHE_LOG_DIR}/admin.access.log combined

</VirtualHost>

<VirtualHost *:80>
	ServerName pga.dev

	ServerAdmin webmaster@localhost
	DocumentRoot /var/www/admin/pga

	#LogLevel info ssl:warn

	ErrorLog ${APACHE_LOG_DIR}/pga.error.log
	CustomLog ${APACHE_LOG_DIR}/pga.access.log combined

</VirtualHost>

<VirtualHost *:80>
	ServerName pma.dev

	ServerAdmin webmaster@localhost
	DocumentRoot /var/www/admin/pma

	#LogLevel info ssl:warn

	ErrorLog ${APACHE_LOG_DIR}/pma.error.log
	CustomLog ${APACHE_LOG_DIR}/pma.access.log combined

</VirtualHost>
